# リポジトリ作業指針

## 目的

Symbol、NEMおよび関連技術に関する仕様書、実装、テスト、レビューを、根拠の区分を明確にしたうえで安全かつ一貫して行う。

一般知識や記憶だけでプロトコルの挙動を断定しない。仕様にない動作を推測で補完せず、確認できない事項は未確認事項として報告する。

## 適用対象

SymbolまたはNEMについて技術的判断を伴う、次の作業に適用する。

- 仕様書の作成およびレビュー
- コードの実装およびレビュー
- テスト設計およびfixture作成
- セキュリティ検証
- 技術ドキュメント作成

## 技術知識ベース

このリポジトリにおけるSymbol、NEMおよび関連技術の標準的な技術資料は、`docs/knowledge` に格納されている。

作業を始める前に対象機能に関連する資料を検索し、必要なファイルだけを参照する。すべての資料を一括で読み込まない。

## 情報源

原則として、次の順序で対象作業に必要な情報を確認する。

1. 対象機能の承認済み仕様書および要件
2. この `AGENTS.md`
3. Architecture Decision Record
4. `docs/knowledge` の関連資料
5. 対象バージョンのプロトコル仕様
6. Catapult、NIS、Catbufferなどの公式実装またはスキーマ
7. 公式SDK
8. 既存コード、テストおよびfixture

既存コードが長期間使用されていることだけを、正しさの根拠にしない。

## 情報の区分

次の情報を混同しない。

- SymbolまたはNEMのプロトコル仕様
- CatapultまたはNISの実装固有動作
- Catbufferスキーマ
- SDKが提供する利便API
- ウォレット固有動作
- このリポジトリ固有の設計判断

仕様書、実装、レビューでは、判断がどの区分に基づくかを明確にする。

## 情報の競合

資料間で記述が競合する場合は、勝手に統合または選択しない。次の項目を確認する。

1. 対象チェーン
2. 対象ネットワーク
3. 対象バージョン
4. 資料の更新日
5. プロトコル仕様か実装固有動作か
6. リポジトリ固有の設計判断が存在するか

競合を解消できない場合は、採用した仮定を事実として扱わず、競合内容と影響を未確認事項として報告する。

## 作業上の原則

- 仕様にない動作を推測で追加しない。
- 外部入力を検証前に信用しない。
- 秘密鍵、ニーモニック、パスワードをログ、例外、デバッグ出力へ含めない。
- ブロックチェーン数量の計算に浮動小数を使用しない。
- SymbolとNEMの処理を暗黙に共通化しない。
- MainnetとTestnetを明示的に区別する。
- 署名対象となる正確なバイト列を明示する。
- プロトコル仕様とSDKの利便APIを同一視しない。
- 規範仕様、スキーマ、実装、テストおよびfixtureの対応を確認する。
- テスト未実行の項目を成功したと報告しない。

## 検証と報告

変更内容に応じて、関連するformatter、lint、型検査、テストおよびビルドを実行する。

作業完了時は、次を区別して報告する。

- 実施した変更
- 判断に使用した仕様および情報の区分
- 実行した検証とその結果
- 実行できなかった検証とその理由
- 残存する仮定、競合および未確認事項
