# リポジトリ作業指針

## このファイルの役割

この `AGENTS.md` は、このリポジトリで作業するエージェントの
作業手順、情報探索、変更範囲、検証および報告方法を定義する。

このファイル自体は、Symbol、NEM、Wallet Core または各種プロトコルの
技術仕様の正本ではない。

技術的な判断を行う場合は、後述する Source of Truth と
対象作業に対応するドキュメントを確認すること。

`AGENTS.md` にプロジェクト固有の詳細仕様や設計判断を蓄積しない。
それらは `docs/requirements/`、`docs/design/`、
`docs/specifications/` などの適切な正本へ記録する。

---

## 目的

Symbol、NEM および関連技術に関する仕様、実装、テスト、レビューを、
根拠の区分を明確にしたうえで安全かつ一貫して行う。

一般知識や記憶だけでプロトコルの挙動を断定しない。

仕様にない動作を推測で補完せず、
確認できない事項、資料間の競合、未決定事項は、
実装や仕様へ勝手に取り込まず明示して報告する。

---

## Repository Map

### プロジェクト概要

- `README.md`
  - 利用者向けの概要
  - 公開 API
  - 利用方法
  - Binding
  - セキュリティ上の注意

### コンセプト資料

- `docs/consept/`
  - プロジェクトの目的
  - 解決対象
  - スコープ
  - 非目標
  - 上位方針

コンセプト段階では、要件、仕様、実装詳細へ不用意に踏み込まない。

### 要件資料

- `docs/requirements/`
  - 承認済み要件
  - 外部から観測可能な要求
  - セキュリティ要求
  - 対応範囲
  - 制約

要件に存在しない仕様や機能を推測で追加しない。

### 仕様書

- `docs/specifications/`
  - 実装対象となる規範仕様
  - データフォーマット
  - API 契約
  - エラー条件
  - セキュリティ動作
  - 相互運用上の規則

実装時は、対象機能について承認済み仕様が存在する場合、
その仕様を外部可視動作の主たる根拠とする。

### 設計資料

- `docs/design/`
  - 基本設計
  - 承認済み設計判断
  - 責務境界、依存方向、trust boundary
  - 設計上の未決定事項と仕様への引継ぎ

設計と設計判断は `docs/design/` を現行正本とする。

対象機能に関連する設計判断が存在する場合は確認する。

新たな重要な設計判断を行う必要がある場合は、
既存の要件・仕様との整合を確認し、
必要に応じて `docs/design/` に記録する。

開発フェーズは次の順序で扱う。

`Concept → Requirements → Design → Specification → Implementation`

設計判断は Design に含め、独立した開発フェーズや別の正本として扱わない。

### 技術知識ベース

- `docs/knowledge/`
  - Symbol
  - NEM
  - Catapult
  - NIS
  - Catbuffer
  - SDK
  - 暗号方式
  - プロトコル仕様
  - その他関連技術

対象機能に関連する資料だけを検索して読む。

すべての資料を一括で読み込まない。

`docs/knowledge/` はプロジェクト固有の要求や仕様を
上書きするものではない。

### レビュー記録

- `docs/reviews/`
  - コンセプトレビュー
  - 要件レビュー
  - 仕様レビュー
  - 実装レビュー
  - その他の検証記録

レビュー結果は要求や仕様そのものではない。

レビュー指摘を修正へ取り込む場合は、
承認済みのコンセプト、要件、仕様、設計判断と
整合していることを確認する。

### Agent Skills

- `.agents/skills/`

作業内容に対応する Skill が存在する場合は、
その Skill の `SKILL.md` を確認し、詳細な作業手順に従う。

代表的な Skill:

- `concept-author`
- `concept-author-multi-agent`
- `concept-review`
- `requirements-author`
- `requirements-author-multi-agent`
- `requirements-review`
- `spec-author`
- `spec-author-multi-agent`
- `spec-review`
- `implement-author`
- `implement-review`
- `readme-author`
- `readme-review`
- `release-readiness-review`

`AGENTS.md` はリポジトリ全体に共通するルールを定義し、
各 Skill は特定作業に対する詳細ルールを定義する。

同じ内容を両方へ大量に重複して記述しない。

---

## 作業開始時の基本手順

作業を開始する前に、次の順序で対象範囲を確認する。

1. ユーザーから与えられた依頼と変更範囲
2. 対象作業に対応する `.agents/skills/` の Skill
3. 対象機能のコンセプト、要件、仕様
4. 対象機能に関連する `docs/design/`
5. 必要な場合のみ `docs/knowledge/`
6. 対象コード、テスト、fixture
7. 必要な場合のみ外部の公式資料または公式実装

対象作業に必要のない文書やコードを、
コンテキスト確保だけを目的として大量に読み込まない。

---

## Source of Truth

### プロジェクト固有の外部可視動作

プロジェクト固有の機能、API、データ形式、制約、
エラー条件、セキュリティ動作などについては、
原則として次を確認する。

1. 承認済み要件
2. 承認済み仕様
3. `docs/design/` の適用可能な設計判断

要件と仕様の役割を混同しない。

要件が「何を満たすか」を定義し、
仕様が「外部から観測可能な動作をどう定義するか」を定めている場合、
実装は承認済み仕様へ従う。

### Symbol / NEM 等の技術的事実

Symbol、NEM または関連プロトコルの技術的事実を確認する場合は、
原則として次の情報を使用する。

1. `docs/knowledge/` の対象機能に関連する資料
2. 対象バージョンの公式プロトコル仕様
3. Catapult、NIS、Catbuffer 等の公式実装またはスキーマ
4. 公式 SDK
5. 既存コード
6. テストおよび fixture

既存コードが長期間使用されていることだけを、
正しさの根拠にしない。

SDK の利便 API が存在することだけを理由として、
その挙動をプロトコル仕様として扱わない。

### 作業指示

次は技術仕様の正本ではなく、作業方法を定義する。

- `AGENTS.md`
- `.agents/skills/`

これらを Symbol、NEM、Wallet Core の技術仕様の根拠として扱わない。

---

## 情報の区分

次の情報を混同しない。

- Symbol または NEM のプロトコル仕様
- Catapult または NIS の実装固有動作
- Catbuffer スキーマ
- SDK が提供する利便 API
- ウォレット一般の慣行
- この Wallet Core 固有の仕様
- このリポジトリ固有の設計判断
- テストまたは fixture に記録された期待値

仕様、実装、レビューでは、
重要な判断がどの区分に基づくものか確認する。

特に次を根拠なく同一視しない。

- Symbol と NEM
- Mainnet と Testnet
- プロトコル仕様と SDK API
- 公式仕様と既存実装
- 一般的なウォレット実装とこのリポジトリの要件
- テストの期待値と規範仕様

---

## 情報の競合

資料間で記述が競合する場合は、
勝手に統合、補完、平均化または選択しない。

次を確認する。

1. 対象チェーン
2. 対象ネットワーク
3. 対象バージョン
4. 資料の更新時点
5. 規範仕様か参考資料か
6. プロトコル仕様か実装固有動作か
7. SDK 固有動作か
8. リポジトリ固有の設計判断が存在するか
9. 後続の設計判断またはレビュー解決記録によって更新されていないか

競合を解消できない場合は、
採用した仮定を事実として扱わない。

競合内容、影響範囲、判断に必要な追加情報を報告する。

外部可視動作に影響する競合を、
実装上の都合だけで決定しない。

---

## Scope Discipline

依頼された範囲を越えて機能を追加しない。

特に次を禁止する。

- 承認済み要件または仕様に存在しない機能の追加
- 仕様に存在しない公開 API の追加
- 仕様に存在しない設定項目の追加
- 仕様に存在しないデータフィールドの追加
- 仕様に存在しない入力制約の追加
- 仕様に存在しないエラー条件の追加
- 仕様に存在しないフォールバックの追加
- 仕様に存在しない互換動作の追加
- 将来の拡張だけを目的とした公開抽象化
- 一般的なウォレットに存在するという理由だけでの機能追加
- 「便利だから」という理由だけでの機能追加
- 「将来必要になるかもしれない」という理由だけでの機能追加

セキュリティ、保守性、相互運用性を改善する目的であっても、
外部可視動作を変更する必要がある場合は、
既存仕様の範囲内か確認する。

仕様にない事項を発見した場合は、
原則として実装で補完せず未決定事項として扱う。

内部実装詳細については、
外部可視動作を変更しない範囲で合理的な判断を行ってよい。

迷った場合は追加しない。

---

## セキュリティ原則

このリポジトリは秘密鍵、Mnemonic、署名などを扱うため、
セキュリティを機能要件と同等以上に扱う。

少なくとも次を守る。

- 外部入力を検証前に信用しない。
- 秘密鍵をログへ出力しない。
- Mnemonic をログへ出力しない。
- Profile password をログへ出力しない。
- 秘密情報を例外メッセージへ含めない。
- 秘密情報を warning へ含めない。
- 秘密情報を debug 出力へ含めない。
- 秘密情報の不要なコピーを増やさない。
- 秘密情報を必要以上に長く保持しない。
- 明示的 export API 以外から秘密情報を返さない。
- 暗号学的乱数が必要な処理に非暗号学的乱数を使用しない。
- 暗号方式、KDF、nonce、salt、鍵長などを独自判断で変更しない。
- 署名対象となる正確な byte 列を確認する。
- 署名前後の byte 列を暗黙に変換しない。
- 失敗時は安全側に失敗する。
- 認証失敗と破損データを推測で復旧しない。
- Wallet Store の不正な内容を信用しない。
- opaque と定義されたデータを呼び出し側が解釈する前提を追加しない。

セキュリティ上重要な挙動が仕様に存在しない場合は、
安全そうな挙動を独自に追加するのではなく、
必要な仕様判断として報告する。

---

## Symbol / NEM 固有の原則

- Symbol と NEM の処理を暗黙に共通化しない。
- Mainnet と Testnet を明示的に区別する。
- Chain と Network の意味を混同しない。
- Symbol と NEM で異なる導出、署名、address、serialization 等を確認する。
- ブロックチェーン数量の計算に浮動小数を使用しない。
- byte order を推測しない。
- hex 表現と raw byte 列を混同しない。
- public key、private key、signature、hash の長さを推測しない。
- Catbuffer スキーマと SDK オブジェクト表現を同一視しない。
- プロトコル仕様と SDK の利便 API を同一視しない。
- 規範仕様、スキーマ、実装、テスト、fixture の対応を確認する。

プロトコル上の挙動を一般知識や記憶だけで断定しない。

---

## 変更時の基本方針

### 既存コード

既存コードを正しさの最終根拠として扱わない。

仕様と既存コードが異なる場合は、
既存コードが仕様に適合しているかを確認する。

既存コードの挙動を維持する必要がある場合は、
その互換性要求に根拠が存在するか確認する。

### 公開 API

公開 API の変更は特に慎重に扱う。

次の変更は、仕様または明示的な要求なしに行わない。

- 関数名
- 引数
- 戻り値
- error code
- warning
- enum
- serialization
- ABI
- WASM 公開 API
- Native Binding
- ownership 契約
- memory 解放契約

### データフォーマット

Wallet Store やその他の永続化・交換フォーマットを変更する場合は、

- version
- backward compatibility
- parser validation
- serialization determinism
- migration
- authentication
- corruption handling

について承認済み仕様を確認する。

### 依存関係

新しい依存関係を追加する前に、
既存依存で実現できないか確認する。

暗号、serialization、乱数、秘密情報処理に関わる依存追加は、
特に慎重に扱う。

不要な依存追加を行わない。

---

## テスト方針

変更した外部可視動作には、
可能な範囲で対応するテストを追加または更新する。

正常系だけでなく、対象に応じて次を確認する。

- 境界値
- 不正入力
- malformed data
- authentication failure
- wrong password
- wrong network
- wrong chain
- truncated input
- invalid length
- duplicate data
- unsupported version
- corruption
- failure path
- secret leakage
- deterministic output
- interop fixture

既存テストが通ることだけを、
変更の正しさの証明として扱わない。

テストが仕様と矛盾する場合は、
テストを正本として仕様を変更しない。

---

## Validation

Rust または Binding の実装を変更した場合は、
原則としてリポジトリルートから以下を実行する。

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
cargo check --target wasm32-unknown-unknown --features wasm
```

変更内容によって追加の検証が必要な場合は、
関連するテスト、ビルド、fixture 検証等も実行する。

Coverage を対象とする作業では、
CI の coverage 設定を確認する。

検証コマンドを実行できなかった場合は、
成功したものとして扱わない。

一部だけ実行した場合は、
実行した範囲と未実行の範囲を区別する。

「おそらく通る」「問題ないはず」を
実行結果として報告しない。

---

## ドキュメント変更

ドキュメントはそれぞれの抽象度を守る。

### コンセプト文書

扱うもの:

- なぜ作るか
- 誰のためか
- 何を解決するか
- スコープ
- 非目標
- 上位制約

詳細 API、field、error code、serialization 等へ
不用意に踏み込まない。

### 要件文書

扱うもの:

- 必要な能力
- 外部要求
- セキュリティ要求
- 制約
- 成功条件

内部実装方式を必要以上に固定しない。

### 仕様文書

扱うもの:

- 外部可視動作
- API 契約
- データ形式
- validation
- error
- protocol behavior
- interoperability
- security behavior

実装内部の不要な詳細へ踏み込まない。

### 設計と設計判断の記録

複数の合理的な選択肢から、
プロジェクトとして一つを選択した理由を `docs/design/` に記録する。

単なる実装メモを ADR として乱立させない。

---

## レビュー方針

レビューでは、
「より良い設計」を自由に提案することよりも、
対象文書または実装が現在の承認済みスコープに適合しているかを優先する。

特に次を区別する。

- 仕様違反
- バグ
- セキュリティ問題
- 相互運用性問題
- 未決定事項
- 改善提案
- 将来拡張

改善提案を必須修正として扱う場合は、
その根拠を明確にする。

レビューによってスコープを暗黙に拡張しない。

レビュー対象より上位の抽象度または
下位の抽象度へ不用意に踏み込まない。

---

## 未決定事項

外部可視動作に関わる未決定事項を、
実装者判断だけで確定しない。

特に次は注意する。

- 公開 API
- データフォーマット
- default
- validation
- error
- security policy
- compatibility
- supported version
- supported algorithm
- protocol behavior

未決定事項が作業を妨げる場合は、

1. 確認できた事実
2. 不足している決定
3. 影響する範囲
4. 選択肢がある場合はその選択肢
5. 作業継続可能かどうか

を分離して報告する。

推測を事実として記録しない。

---

## Git と変更範囲

ユーザーから明示的な指示がない限り、
無関係なファイルを変更しない。

既存のユーザー変更を勝手に取り消さない。

次を避ける。

- 無関係な formatting
- 大規模 rename
- 不要なファイル移動
- 不要な dependency update
- Cargo.lock の無関係な変更
- 自動生成物の不要な更新
- レビュー記録の勝手な削除
- OPEN 項目の根拠なき close

作業に必要な最小十分な変更を優先する。

---

## 作業完了時の報告

作業完了時は、必要に応じて次を区別して報告する。

### Changes

- 実施した変更
- 変更した主要ファイル
- 外部可視動作への影響

### Evidence

- 判断に使用した要件
- 判断に使用した仕様
- 判断に使用した設計判断
- 必要に応じて参照した技術資料

### 検証結果

- 実行した formatter
- 実行した lint / clippy
- 実行したテスト
- 実行した build / check
- それぞれの結果

### Not validated

- 実行できなかった検証
- 実行できなかった理由

### Remaining issues

- 残存する仮定
- 未決定事項
- 資料間の競合
- 未確認事項
- 今後必要なレビュー

問題が存在しない項目を、
形式だけのために無理に生成する必要はない。

---

## 禁止事項

以下を行わない。

- 仕様にない挙動を事実として断定する
- 記憶だけを根拠にプロトコルを実装する
- 未実行のテストを成功したと報告する
- 未確認の仕様を確認済みとして扱う
- 競合資料を勝手に統合する
- 秘密情報をログまたは報告へ含める
- 便利さだけを理由にスコープを拡張する
- 将来拡張だけを理由に公開 API を増やす
- レビュー指摘だけを根拠に仕様外機能を追加する
- 既存コードだけを根拠に仕様を推定する
- SDK の挙動だけをプロトコル仕様とみなす
- Symbol と NEM を同一仕様として扱う
- Mainnet と Testnet を暗黙に混在させる
- エラー発生時に秘密情報を診断情報へ含める

---

## 基本原則

最小十分な変更を行う。

確認できた事実と推測を分離する。

仕様と実装を分離する。

プロトコル仕様と SDK を分離する。

Symbol と NEM を分離する。

Mainnet と Testnet を分離する。

未決定事項を勝手に決定しない。

秘密情報を漏らさない。

テストしていないものを成功したと報告しない。

迷った場合は、
機能を追加するのではなく根拠を確認する。
