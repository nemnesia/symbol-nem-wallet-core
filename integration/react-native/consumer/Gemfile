source 'https://rubygems.org'

# The producer graph is intentionally tied to one Ruby patch release. The
# matching Gemfile.lock is part of the source-controlled consumer input.
ruby "3.3.8"

gem "cocoapods", "1.16.2"
gem "activesupport", "7.2.2.2"
gem "xcodeproj", "1.27.0"
gem "concurrent-ruby", "1.3.3"

# These libraries are no longer guaranteed to ship with every supported Ruby.
gem "bigdecimal", "3.1.8"
gem "logger", "1.6.2"
gem "benchmark", "0.4.0"
gem "mutex_m", "0.3.0"
gem "nkf", "0.2.0"
