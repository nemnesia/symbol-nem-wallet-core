# RN 0.87.0 CocoaPods input. The package path is supplied only by the
# producer/consumer checkout and is never resolved from an untracked path.
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, min_ios_version_supported
prepare_react_native_project!
use_frameworks! :linkage => :static

target 'SnwcRnBuild' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath],
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  pod 'SymbolNemWalletCoreRN', :path => ENV.fetch('SNWC_RN_POD_PATH', '../../../../packages/wallet-core/ios')

  post_install do |installer|
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false,
    )
  end
end
